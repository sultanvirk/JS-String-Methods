const str = 'I do code and i love to code'

console.log(str.indexOf('code')) // indexOf method returns the first finded element, return -1 if element not found
console.log(str.lastIndexOf('code')) //lastIndexOf retuns the last targeted element in array, if the targeted element is not twise in array it will return the values same as indexOf method